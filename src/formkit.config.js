import { es } from '@formkit/i18n'
import { genesisIcons, eyeClosed } from '@formkit/icons'

const config = {
  locales: { es },
  locale: 'es',
  icons: {
    ...eyeClosed,
    genesisIcons,
    

  }
}

export default config
